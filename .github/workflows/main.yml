name: CI

on:
  push:
    branches:
    - CI-test

jobs:
  build:
    runs-on: ubuntu-latest  
    steps: 
    - name: Use Node.js 10
      uses: actions/setup-node@v1
      with:
        version: 10

    - name: npm install, build and test
      run: |
        npm install
        npm run build --if-present
 
    - name: zip the archive
      uses: montudor/action-zip@v0.1.0
      args: zip -r deployment.zip ./build
